{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npx prisma generate --schema=server/prisma/schema.prisma && DATABASE_URL=file:./server/prisma/dev.db npx prisma db push --schema=server/prisma/schema.prisma --accept-data-loss --skip-generate && npm run build"
  },
  "deploy": {
    "startCommand": "node server/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
